# 引擎加载与渲染

离屏 `Engine` 加载模型、渲染一帧、与黄金图比对。窗口几何与通道 JSON 对齐见 [多通道同步模块设计.md](../多通道同步/多通道同步模块设计.md) §3.1 / §11 `CFG-window-matches-channel`。实体装配见 [实体管理设计.md](./实体管理设计.md)。

## 目录

1. [范围](#1-范围)
2. [验收要点](#2-验收要点)

---

## 1. 范围

- 有效模型路径：`init` 成功且 `tickOnFrame` 出一帧
- 模型不存在：`init` 失败
- 默认茶壶视角与黄金图一致（改参考图须显式更新，用例挂 `[golden]`）

## 2. 验收要点

> 对齐 [测试用例书写规范.md](../../测试用例书写规范.md)。对照 `RenderingTests.cpp`。码一经分配不改号、不复用、不重排。Catch2 挂同名 tag。

| 码 | 场景 | 验收 | Catch2 |
| --- | --- | --- | --- |
| `RND-load-frame` | 加载并渲染 | 有效 teapot 模型 `init` 成功且渲染一帧 | `[acceptance][bdd][render][load][RND-load-frame]` |
| `RND-load-missing` | 路径不存在 | `init` 失败 | `[acceptance][bdd][render][load][failure][RND-load-missing]` |
| `RND-golden-teapot` | 黄金图 | 默认茶壶视角与参考图一致 | `[acceptance][bdd][render][golden][RND-golden-teapot]` |
