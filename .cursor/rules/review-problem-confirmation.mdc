---
description: 审查（review）发现问题时，先登记到设计文档「问题自查」节、逐条讨论确认后再改，不直接修改正文
alwaysApply: true
---

# 审查问题确认（review → 登记 → 确认 → 改）

对设计文档 / 代码做**审查**时，发现的问题**不得直接修改正文**——先登记、报告、讨论确认，再处理。

## 核心规则

「审查请求」（如「检查一下是否还有漏洞」）**只产出问题清单，不产出改动**。审查 ≠ 授权修改。

## 流程（写死）

1. **登记**：把所有审查发现的问题列到设计文档的「问题自查」节（表格：`#` / 问题 / 关联章节 / 状态）。
2. **报告**：向用户报告问题清单，并按性质分类标注——
   - **客观矛盾 / 事实错误**（文档内部自相矛盾、与代码不符）
   - **设计决策 / 缺口**（需用户拍板的语义选择）
3. **讨论**：逐条与用户讨论确定方案。**用户是决策方，AI 不擅自定方案**（尤其是设计决策类）。
4. **处理**：方案确认后才修改正文；已解决的从「问题自查」节删除或标记「已解决」。

## 例外——可直接改、无需登记

- 拼写 / 明显 typo。
- 用户明确说「直接改 / 你决定就好」。

## 示例

```markdown
## 14. 问题自查（待讨论）

| # | 问题 | 关联章节 | 状态 |
| --- | --- | --- | --- |
| 1 | §1 与 §11 对「父子挂载」是否首版实现的说法矛盾 | §1 / §11 | 待讨论 |
| 2 | 重试重发 Active 会误激活 Standby 实体 | §7.1 | 待讨论 |
```
