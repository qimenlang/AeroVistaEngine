---
description: 代码（非注释）与文档不得放进同一个 commit；注释跟着文档走；同一 PR 即可
alwaysApply: true
---

# 代码与文档分 commit

约束 **AI 被要求提交时** 如何切 commit。不靠 git hook；人手提交自行遵守。未要求提交则不要主动 commit。

## 硬约束

同一 commit 里不得同时包含：

- **文档**：`doc/`（设计契约、notes、测试规范等）
- **非注释源码**：可执行行为 / API / 协议 / 配置语义的改动（含新建、删除源文件）

判定看 **diff 是否改了可执行行为**，不是看扩展名是不是 `.cpp`。

两次提交可以进 **同一个 PR**（GitHub Pull Request；GitLab 称 MR，同一概念）。

## 怎么切

| 这次实际改了什么 | commit |
| --- | --- |
| 只改 `doc/` | 1 |
| 改 `doc/`，并改源码/测试里的注释、章节引用（无逻辑） | **1**（算文档提交） |
| 改引擎行为 / API / 协议，再同步设计文档 | **2**（代码一次，文档一次） |
| 行为改了，注释也改了，文档也改了 | **2**（注释跟代码走；`doc/` 单独） |

顺序：先代码 commit，再文档 commit（文档描述已落地的行为）。

## 例外

- 用户明确说「就一个 commit / 不用拆」。
- 只动一边（纯文档，或纯代码且无 `doc/` 改动）。
