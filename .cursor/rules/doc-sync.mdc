---
description: 文档同步——改代码/改文档后保持文档与代码一致；决策日志只读（硬约束）
globs: "**/*.{cpp,h,hpp,md}"
alwaysApply: true
---

# 文档同步（doc-sync）

「设计即契约」：`doc/design/*.md` 与实现必须一致；`doc/` 下各文档之间的相互引用也必须一致。改动之后，不允许留下指向旧名/旧路径/旧编号的失效引用。

## 触发（何时需要同步）

- 改动引擎业务逻辑（协议、线程模型、接口签名、载荷布局、时序、执行模型、网络收发）——不只新功能，**重构、修 bug 改行为**同样适用。
- 更新 `doc/` 下任一文档（新增 / 删除 / 改名 / 改章节标题 / 改文件名）。

**详细核查步骤、命令、报告格式 → 交给 skill：`/doc-sync`（实体在 `.claude/skills/doc-sync/`）**。机械核查由 `scripts/doc_sync_check.py` 承担；「是否触及、怎么改、命中是否属说明性上下文」由你判断。

## 硬约束：决策日志只读（必须始终在场）

「决策日志」是**用户亲手维护**的文档，AI 只读、不修改、不追加、不演进。

### 识别方式（按文件名精确匹配，不按内容）

- 文件名含「决策日志」四个字（如 `决策日志.md`），或
- 位于 `**/决策日志/` 目录下。

> 按文件名 / 目录匹配，而不是按「内容是否含决策」判断——设计文档（`状态同步设计.md`、`多通道同步模块设计.md`）里大量出现「决策」「否决」字样，按内容会误伤这些本应由 AI 演进的设计文档。

### 规则

1. AI 产出的方案与决策写入**设计文档**（`doc/design/`），由用户审核。
2. 用户审核后，**自行把最终决策摘记到决策日志**（亲手写，这是掌控力的来源）。
3. AI 不得在任何情况下改写、追加、删除决策日志内容——哪怕用户要求「同步」或「更新」，也应提醒用户「决策日志由你亲手维护」。
