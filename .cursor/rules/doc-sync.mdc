---
description: 文档同步——重构业务逻辑时同步设计文档；更新任一文档时同步关联文档；决策日志只读
globs: "**/*.{cpp,h,hpp,md}"
alwaysApply: true
---

# 文档同步（doc-sync）

本文档分三部分：**§A 重构/改代码时**同步设计文档，**§B 仅更新文档时**同步关联文档，**§C 决策日志只读**。共同原则是「改动之后，所有与之关联的文档都要一致；决策日志由用户亲手维护，AI 只读」。

---

## A. 重构/改代码时：同步设计文档

Authority: `doc/design/*.md` 是「设计即契约」，与实现必须一致。

### 触发时机

**每次改动引擎业务逻辑**（协议、线程模型、接口签名、载荷布局、时序、执行模型、网络收发），
不只是新功能——**重构、修 bug 改行为、收敛/合并机制**同样适用。

### 必须同步核查的文档

**`doc/design/` 下的所有设计文档**——本次改动可能影响任何一份（不只是命令面）。

典型受影响（但不限于）：
- 本次改动涉及的子模块设计（命令面、时钟同步、lla 位姿、显示/帧同步等）
- 被新实现改写的通用机制所在文档（断线检测、双平面、载荷布局）
- `doc/notes/` 下被新实现推翻的通用知识（如 socket 断线检测、线程模型）

核查方式：改动后**列出 `doc/design/` 全部文档**，逐一判断「本次改动是否触及该文档描述的行为/协议/API」；触及则更新，未触及则跳过。

### 核查清单（改完代码后逐项）

1. **签名**：新增/改动的公开 API（`sendCommand`、回调、注入接口）签名与文档 §API 表一致。
2. **载荷布局**：协议字节布局（如 `[id(4B)][path…]`）与文档 §2.2 一致，两端（测试 helper 与实现）一致。
3. **执行/线程模型**：§4 执行模型描述（如「命令读循环线程 + 主线程执行」）与实际线程结构一致；目录锚点文本 = 实际 § 标题。
4. **行为决策**：写死的设计决策（如「超时即失败」「执行归主线程」）有文档记录；废弃机制（如独立执行线程）标注「已否决」而非残留旧描述。
5. **实现状态表**：§11「与实现关系」的「待实现/已实现」反映当前实现。
6. **跨文档引用**：初版/完整版/模块设计的相互引用与措辞一致（顶部声明、扩展位标注）。
7. **已删除符号**：文档不得引用已删除的类/函数名（如 `isTcpPeerAlive`）；用行为描述替代。

### 硬性要求

- 代码逻辑改动完成后、交差前，**先跑 `rg` 搜旧符号/旧行为是否残留在 `doc/design/` 全部文档**：
  `rg "旧函数名|旧术语|旧行为描述" doc/design`
- 找不到对应文档段落时，**主动补充**到相关文档（设计决策写「写死」）。
- 纯命名/格式改动（不改变行为）不需要同步文档；但**目录锚点**只要 § 标题变了就同步。

### 验证

```text
# 提交前自查示例
rg "双线程执行|独立执行线程|isTcpPeerAlive|refreshConnectionState" doc/design
# 期望：无输出，或输出为「已否决/已删除」的说明性上下文
```

---

## B. 仅更新文档时：同步关联文档

### 触发时机

**每次更新 `doc/` 下任一文档**（新增、删除、改名、改章节标题、改文件名），都要检查「是否有其他文档引用了它」，有则一并同步。

### 必须核查的关联类型

1. **索引文件**：`doc/private/README.md` 是 private 目录的索引，其「文件清单」必须与实际文件一致——新增/删除/改名文档都要更新清单。
2. **交叉引用链接**：private 各文档之间相互引用（决策结论 ↔ 技术地图 ↔ 具身智能方向 ↔ 日常工作方式 ↔ AI 竞争力），路径随文件移动/改名而变化，须同步。
3. **目录锚点 / 章节编号**：某文档的 § 标题或编号变了，指向它的锚点文本、以及其它文档里「详见 §X」的引用要同步。
4. **文件移动/改名**：移动或改名后，所有指向旧路径的链接都要改。

### 核查方式

- 更新文档后，`rg` 该文档的**文件名、标题、章节编号**，确认所有引用处一致：
  `rg "被改文件名|被改标题|§数字" doc/`
- 触及关联文档则更新，未触及则跳过。

### 硬性要求

- 新增文档后，立即登记到对应目录的索引（如 `README.md`）。
- 移动/改名文档后，修正所有指向它的链接。
- 章节标题/编号变化后，同步所有「详见 §X」与锚点引用。

---

## C. 决策日志只读规则

「决策日志」是**用户亲手维护**的文档，AI 只读、不修改、不追加、不演进。

### 识别方式（按文件名精确匹配，不按内容）

- 文件名含「决策日志」四个字（如 `决策日志.md`），或
- 位于 `**/决策日志/` 目录下。

> 按文件名/目录匹配，而不是按「内容是否含决策」判断——设计文档（`状态同步设计.md`、`多通道同步模块设计.md`）里大量出现「决策」「否决」字样，按内容会误伤这些本应由 AI 演进的设计文档。

### 规则

1. AI 产出的方案与决策写入**设计文档**（`doc/design/`），由用户审核。
2. 用户审核后，**自行把最终决策摘记到决策日志**（亲手写，这是掌控力的来源）。
3. AI 不得在任何情况下改写、追加、删除决策日志内容——哪怕用户要求「同步」或「更新」，也应提醒用户「决策日志由你亲手维护」。

---

## 通用原则

- 文档与文档之间、文档与代码之间，**改动后必须一致**，不允许留下指向旧名/旧路径/旧编号的失效引用。
- 自查用 `rg`，不要凭记忆判断「应该没别处引用」。
