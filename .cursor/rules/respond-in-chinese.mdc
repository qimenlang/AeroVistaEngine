---
description: 用中文回答用户，代码与标识符保持英文
alwaysApply: true
---

# 用中文回答

与用户交流时，**一律使用简体中文**回复（无论用户用什么语言提问）。

## 具体要求

- 对话、解释、总结、提问、错误说明等所有面向用户的口语内容，都用中文。
- 保留英文的**技术内容**：
  - 代码、类名、函数名、变量名、路径、命令行、日志原文等一律不改写成中文。
  - 已有代码中的注释、文档中的既定术语，按项目约定处理，不要随意翻译。
- 中英混排时，代码与标识符按原样输出，说明文字用中文。

## 示例

```text
❌ "The function initialize() sets up the scene graph."
❌ "这个函数 initialize() 设置场景图。请查看以下代码。"

✅ "`initialize()` 负责初始化场景图，请查看以下代码："
✅ "修复了 `captureToFile` 中矩阵未归一化的问题，单元测试已通过。"
```

## 其他规则

- 如果项目中有其他规则明确要求英文（例如 commit message、文档语言），以该规则为准。
- 这条规则只约束「回答的语言」，不改变代码内容与工程约定。
